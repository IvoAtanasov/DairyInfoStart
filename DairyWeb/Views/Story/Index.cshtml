@model IEnumerable<DairyWeb.ViewModels.StoryInfo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Добави история", "Create")
</p>
@*<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StoryDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Rate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StoryText)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ImageUrl)
        </th>
        <th></th>
    </tr>*@

@foreach (var item in Model) {
    <fieldset>
        <legend>@Html.DisplayFor(modelItem => item.Title)</legend>
                <span><img class="ProfilPicture" src="@item.ImageUrl" /></span>
        <span>
          @Html.DisplayFor(modelItem => item.StoryDate) 

          @Html.DisplayFor(modelItem => item.Rate)
        </span>
               
        <span>  @Html.DisplayFor(modelItem => item.StoryText)</span>
                
                    @*@Html.DisplayFor(modelItem => item.ImageUrl)*@
                @*@Html.ActionLink("Edit", "Edit", new { id=item.StoryID }) |
                @Html.ActionLink("Details", "Details", new { id=item.StoryID }) |*@
                    @*@Html.ActionLink("Delete", "Delete", new { id = item.StoryID })*@
        <a href="@Url.Action("Delete", "Story", new { id = item.StoryID })">
            <img src="~/Content/Images/Delete.gif" />
        </a>

</fieldset>
}

            @*</table>*@
